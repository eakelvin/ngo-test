type FooterValues = {
    email: string
};

type ContactValues = {
    name: string
    email: string
    message: string
};

type Team = {
    avatar: string
    name: string
    title: string
    linkedin: string
    twitter: string
};

type DonateValues = {
    name: string
    number: number
    email: string
    amount: number
};